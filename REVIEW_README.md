# Anonymous review snapshot

This archive includes the application, tests, experiment scripts, and the
[paper appendix](docs/PAPER_APPENDIX.md).

Download and extract the archive, then follow README.md for installation.
Personal filesystem paths and deployment addresses have been replaced with
generic examples. Addresses in 192.0.2.0/24 are documentation placeholders;
configure your own simulator and deployment endpoints before running remote
helpers. The application can run with MOCK dynamics without an AirSim server.

Original third-party citations and the existing MIT license are retained.
Local credentials, runtime data, untracked files, and Git history are excluded.
